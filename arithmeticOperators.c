#include <stdio.h>

int main(){
    //arithmetic operators

    // + (addition)
    // - (subtraction)
    // * (multiplation)
    // / (division)
    // % (modulus)
    // ++ increment
    // -- decrement

    int x = 5;
    int y =2;
    int z = x + y;
    float w = x / (float) y; //since we can only put whole numbers on int "int"

    printf("%d", z);
    printf("\n%f", w);
    return 0;

}