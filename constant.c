#include <stdio.h>

int main(){
    //constant = fixed value that cannot be altered by the program during its execution

    const float pi = 3.14159; // so that it cannot bbe change
   // pi = 420.69; // see error that it cannot be changed

    printf("%f",pi);
    return 0;
}